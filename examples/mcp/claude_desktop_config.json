{
  "_comment": [
    "Claude Desktop / Claude Code MCP configuration for Ratchet.",
    "Put the API key in `env`, never in `args` \u2014 arguments are visible in process listings.",
    "Get a key at https://ratchet-gate.fly.dev/console (free, no card).",
    "macOS path:   ~/Library/Application Support/Claude/claude_desktop_config.json",
    "Claude Code:  .mcp.json in the project root, or `claude mcp add`."
  ],
  "mcpServers": {
    "ratchet": {
      "command": "npx",
      "args": [
        "-y",
        "ratchet-mcp"
      ],
      "env": {
        "RATCHET_API_KEY": "rk_live_replace_me"
      }
    }
  }
}
